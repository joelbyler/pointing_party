Start a new session
<%= form_for @conn, party_path(@conn, :create), [as: :party, class: "form-inline"], fn f -> %>
  <%= text_input f, :for, class: "form-control" %>
  <%= submit "Create", class: "btn btn-primary" %>
<% end %>
